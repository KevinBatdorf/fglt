node_modules
.env
.env.local
data
bun.lock
*.log

# Claude Code per-user permissions
.claude/settings.local.json

# Playwright screenshot/snapshot artifacts
.playwright-mcp/
seg-home.png
*.screenshot.png

# Build artifact: copied from repo root by `apps/desktop/package.json`
# build script so Electrobun can pick it up via `build.copy`. Canonical
# version lives at the repo root.
apps/desktop/assets/docker-compose.consumer.yml
