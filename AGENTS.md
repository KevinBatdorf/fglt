# Steam Library API

Same content as [CLAUDE.md](CLAUDE.md) — kept in sync for tooling that reads
the AGENTS.md convention (Codex, Cursor, etc.) instead of CLAUDE.md.

See CLAUDE.md for: commands, stack, project structure, containers, status
check, workflow, and conventions.
